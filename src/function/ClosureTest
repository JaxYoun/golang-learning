//闭包概念与测试
//匿名函数 + 被匿名函数引用的局部变量构成的应用上下文，外层函数内层匿名函数
package main

import "fmt"

func main() {
  slice := []int{1, 2, 3, 4}
  fmt.Println(OuterFunc()(slice...))  //此处的切片作为可变参数集合，需要使用省略号
}

func MyClosure() func(...int) int {
  sum := 0
  fun := func(slice ...int) int {  //匿名内部函数接收可变参数
    for _, v := range slice {
      sum += v
    }
    return sum
  }
  return fun
}

